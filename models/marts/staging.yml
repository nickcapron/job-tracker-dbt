version: 2

models:
  - name: stg_jobs
    columns:
      - name: job_id
        tests:
          - unique
          - not_null
      - name: company_name
        tests:
          - not_null

  - name: stg_applications
    columns:
      - name: application_id
        tests:
          - unique
          - not_null
      - name: job_id
        tests:
          - not_null