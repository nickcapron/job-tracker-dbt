version: 2

sources:
  - name: raw
    schema: raw
    description: "Raw data landing zone - populated by ingestion pipeline and web app"
    tables:
      - name: jobs
        description: "Job postings ingested from external job board APIs"
      - name: applications
        description: "User applications written by the web app"
      - name: user_profile
        description: "User profile including skills and job preferences"